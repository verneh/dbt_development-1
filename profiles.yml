config:
  send_anonymous_usage_stats: False

default:
  outputs:

    dev: # User-Password config
      type: snowflake
      account: DBT_CLOUD_DEV
      user: DBT_CLOUD_DEV
      password: Abc12345
      role: TRANSFORMER_DEV
      database: DEV
      warehouse: TRANSFORMING
      schema: DBT_CLOUD_DEV
      threads: 2
      client_session_keep_alive: False

  target: dev